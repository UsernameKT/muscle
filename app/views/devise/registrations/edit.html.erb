<h2>ユーザー編集</h2>

<%= form_with model: resource,
              url: registration_path(resource_name),
              method: :put,
              local: true do |f| %>

  <div>
    名前<br>
    <%= f.text_field :name %>
  </div>

  <div>
    メールアドレス<br>
    <%= f.email_field :email %>
  </div>

  <div>
    自己紹介（任意・160文字まで）<br>
    <%= f.text_area :bio, rows: 4, placeholder: "例）筋トレ歴3年。好きな言葉はパンプアップ" %>

  </div>

  <div>
    新しいパスワード（変更しない場合は空欄）
    <% if @minimum_password_length %>
      （<%= @minimum_password_length %>文字以上）
    <% end %>
    <br>
    <%= f.password_field :password %>
  </div>

  <div>
    パスワード確認<br>
    <%= f.password_field :password_confirmation %>
  </div>

  <div>
    現在のパスワード（変更確認用）<br>
    <%= f.password_field :current_password %>
  </div>

  <%= f.submit "更新する" %>
<% end %>

<h3>退会</h3>
<%= button_to "アカウントを削除",
              registration_path(resource_name),
              method: :delete,
              data: { confirm: "本当に退会しますか？" } %>

<%= link_to "戻る", user_path(current_user) %>
