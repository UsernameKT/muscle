

<% if current_user&.guest_user? %>
  <p>ゲストユーザーは閲覧できません</p>
<% else %>

    <%= render "follow_list",
      title: "#{@user.name}のフォロワー",
      users: @users,
      empty_message: "フォロワーはいません",
      user: @user %>
<% end %>

