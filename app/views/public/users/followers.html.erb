<h2><%= @user.name %> のフォロワー</h2>

<% if @users.any? %>
  <% @users.each do |u| %>
    <div>
      <%= link_to u.name, user_path(u) %>
    </div>
  <% end %>
<% else %>
  <p>フォロワーはいません</p>
<% end %>
<p>
  <%= link_to "← マイページに戻る", user_path(@user) %>
</p>
