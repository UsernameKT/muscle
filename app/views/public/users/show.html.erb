<h1>マイページ</h1>

<p>名前: <%= @user.name %></p>
<p>Email: <%= @user.email %></p>

  <% if user_signed_in? && !current_user.guest_user? %>
    <p><%= link_to "プロフィール編集", edit_user_registration_path %></p>
    <p><%= link_to "退会する", registration_path("user"), method: :delete, data: { confirm: "本当に退会しますか？"} %></p>
    <% end %>
  

